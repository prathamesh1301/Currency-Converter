# Currency-Converter
This is a Currency converter android app in which it will ask for a number in the beginning then after this it has a spinner(drop down list) which will ask for its current currency and then another spinner to convert it into selected spinner option.
At end there is a button named convert which will convert it and display it at the bottom of screen as "Amount:(random number)".This app can convert in 7 different currencies.
